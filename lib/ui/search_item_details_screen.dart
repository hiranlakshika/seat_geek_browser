import 'package:flutter/material.dart';

class SearchItemDetailsScreen extends StatelessWidget {
  const SearchItemDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
